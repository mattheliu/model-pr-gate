# 隐私说明

[English](PRIVACY.md) · **简体中文**

本地命令只读你指定的 session 文件。不联网、不扫描其他文件夹、不写缓存、不收集遥测。逐行解析后丢弃原始记录，不保存对话。为了计数和关联，内存中会保留请求与轮次 ID 的散列，所以内存会随唯一 ID 数量增加。

命令行输出模型标签、汇总计数、证据完整性和白名单检查结果，不输出提示词、代码、工具参数、文件路径、session ID、request ID、response ID 或时间戳。模型名仍然可见；如果你的内部模型名称保密，分享结果前也要检查。

GitHub Action 只输出固定的结果和证据级别，不把详细 JSON 写入 CI 日志，不上传附件。安装 CLI 时会从 GitHub 下载公开工具代码，运行检查时离线。

仓库没有真实 session、凭据或个人活动报告。测试使用合成数据，示例凭据只是占位。不要提交自己的会话或签名私钥。

可选 GitHub App 是另一个程序，只有配置并启动后才会接收 GitHub webhook、从 GitHub 读取 PR 正文和元数据、向 GitHub 写入检查结果。它不上传本地 session。如果只需要离线检查，只运行 CLI 即可。

本地日志可能被修改、截断或遗漏。模型标签符合白名单，只是一项观察结果，不是 PR 真实来源的认证。
