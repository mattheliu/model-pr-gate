# 隐私说明

[English](PRIVACY.md) · **简体中文**

## 默认：验证最小证明

Action 读取 GitHub 已经提供给 runner 的 PR 事件文件，其中本来就包含 PR 元数据和正文。工具只在内存中提取证明，不检出代码、不上传 session、不写缓存、不启动服务、不收集遥测、不发 API 或网络请求。下载 Action 或安装软件包时，仍会正常下载公开工具代码。

证明只包含模型 ID、提交 SHA、签发方、仓库、PR 编号、格式版本和签名，不包含对话、代码、个人路径、session/请求 ID 或时间戳。签发和验证都会拒绝额外字段。证明可解码，不是加密：能读 PR 的人会知道使用的模型。签发方应使用非个人化名称。

CI 日志只有固定的结果、原因和证据级别。工作流和平台仍按原有权限接触 PR；工具不会改变 GitHub 本身的数据处理方式。

签名私钥不应出现在 PR 或验证任务中，由可信生成平台保存。该平台原本处理的模型请求仍遵循它自己的政策，本项目不增加请求代理。

## 可选 DeepSeek Harness 插件

DSH 插件只读取当前 Agent 配置的模型 ID、明确传入的最小证明和 PR 信息，以及
自带的 skill 文件。不查看 session 或 API 凭据，不拦截请求、不调用 GitHub、
不上传、不埋点，也不保存历史。输出只有固定结果和对应语言的解释。
签发方公钥放在本地插件配置中，不需要签名私钥。安装时正常下载公开代码包。

DSH 可能把工具参数和结果记录到自身会话日志，并作为上下文发给所选模型。
只传最小证明，不传 session、完整 PR 正文或秘密。插件不改变宿主和提供商
的数据处理方式。模型配置预检仅作提示，不提供签名证据。

## 可选：本地日志审计

`model-session-audit` 只打开明确指定的文件，逐行处理，不扫描其他目录、不联网。解析后丢弃原始记录，只在内存中保留请求/轮次 ID 的散列用于计数，内存随唯一 ID 数量增加。

命令行 JSON 包含模型标签和汇总计数，不包含提示词、代码、路径或原始 ID。内部模型名称仍可能出现在结果中。请把原始 session 留在本地，不要为了运行命令而上传到 CI。

可选的日志审计 Action 只打印固定结果和证据级别，它与默认验签工具分开，也不能认证真实来源。仓库不包含真实 session、凭据或个人活动报告，测试使用合成数据。v0.3 没有第三方 App 服务。
